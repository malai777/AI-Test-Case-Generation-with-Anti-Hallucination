# Taste
- Generates test cases strictly from the given PRD document only, without inventing or hallucinating extra requirements. Confidence: 0.9
- Prefers exhaustive coverage — "all possible test cases" — when asked to generate from a PRD. Confidence: 0.8
- Saves generated test cases as Markdown files in a dedicated `generated-testcases/` directory, using descriptive snake_case filenames (e.g., `vwo_login_testcases.md`). Confidence: 0.9
- When AI-generated test cases are wrong or hallucinated, investigates the source PRD/requirements to find the missing facts causing the error, cross-referencing test cases against the source document rather than patching the output in isolation. Confidence: 0.7
- Prefers work products to be saved as files in the project folder so they persist across sessions and don't have to be re-run or regenerated — including extracted/intermediate artifacts (e.g., PDF text of the PRD) and analysis outputs (e.g., missing-facts cross-reference reports, saved to `generated-testcases/`). Confidence: 0.9
- Runs controlled A/B experiments on AI-generated test cases (generation with vs. without the anti-hallucination rule), keeping the full artifact trail as numbered Markdown files — original prompt, baseline output without the rule, controlled output with the rule, and an analysis report — so raw outputs are preserved as evidence. Confidence: 0.8
- Wants the project README.md maintained as a living document that reflects what has actually been done in the project (file layout, findings) — grounded in the real project state verified via directory reads, NOT copied from a provided reference document or template; explicitly rejected a README that mirrored the pasted reference doc ("dont write things from the reference one"). Confidence: 0.9
- Prefers tight scope: does only what was explicitly requested and does not create extra files or artifacts beyond the ask — rejected a proposed plan that added prompt/analysis files, wanting only the two requested deliverables (test cases without the rule, README update). Confidence: 0.8
